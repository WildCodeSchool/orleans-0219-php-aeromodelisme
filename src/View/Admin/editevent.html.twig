{% extends 'adminlayout.html.twig' %}

{% block content %}
    <section class="ftco-section mt-4">
        <div class="container text-center">
            <div class="row justify-content-center">
                <div class="history">
                    <h3 class="title3 pb-4">Modifier l' evenement</h3>
                    <h4 class="title4">{{ event.title }}</h4>
                    {% include 'Admin/_formevent.html.twig' %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}